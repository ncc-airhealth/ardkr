# S3 호환 오브젝트 스토리지 접근 (현재 Cloudflare R2). 실제 값은 .env(git-ignored)에 채운다.
# 버킷명·엔드포인트는 비밀이 아니므로 실제 값을 커밋한다
# (knowledge/decisions/infrastructure.md 참고). 키는 절대 여기 채우지 않는다.
GEOVARS_S3_BUCKET_NAME=geovars
GEOVARS_S3_ENDPOINT_URL=https://0338bb0b6b16f3ee66ce6c88e715f9a5.r2.cloudflarestorage.com
GEOVARS_S3_ACCESS_KEY_ID=
GEOVARS_S3_SECRET_ACCESS_KEY=
